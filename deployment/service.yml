kind: Service
apiVersion: v1
metadata:
  name: metabolic-ninja-production
spec:
  selector:
    app: metabolic-ninja
    env: production
  ports:
  - name: api
    protocol: TCP
    port: 80
    targetPort: 8000
